// NoH8 background service worker (Manifest V3).
// Placeholder lifecycle + messaging bridge. The on-device ML pipeline and
// message routing will be wired up in a later phase.
console.info('[NoH8] background service worker loaded');