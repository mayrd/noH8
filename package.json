{
  "name": "noH8-extension",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "vitest": "^1.5.0",
    "typescript": "^5.0.0"
  }
}
